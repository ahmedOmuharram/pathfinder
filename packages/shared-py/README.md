## `pathfinder-shared` (Python) — `packages/shared-py`

Shared Python models (Pydantic) used across the backend and tooling.

### What lives here

- Python package: `src/shared_py/`
- Build output (gitignored): `dist/`

### Development

This package is installed as part of the API environment (see `apps/api/pyproject.toml`).

