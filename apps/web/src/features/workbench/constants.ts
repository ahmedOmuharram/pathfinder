/**
 * VEuPathDB universal constants for controls search.
 *
 * GeneByLocusTag is the standard search available across all VEuPathDB sites
 * (PlasmoDB, ToxoDB, CryptoDB, FungiDB, TriTrypDB, VectorBase) for looking up
 * genes by locus tag / ID list.  The `ds_gene_ids` parameter accepts a dataset
 * ID containing the gene IDs.
 */
export const CONTROLS_SEARCH_NAME = "GeneByLocusTag";
export const CONTROLS_PARAM_NAME = "ds_gene_ids";
