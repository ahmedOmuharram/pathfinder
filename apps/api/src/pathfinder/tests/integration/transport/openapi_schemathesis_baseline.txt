# Operations whose Schemathesis fuzz checks currently fail. Each line is
# a Schemathesis operation label of the form ``METHOD /path/template``.
# Operations listed here are marked xfail(strict=False) so they do not
# break the suite while the underlying issues are being addressed.
#
# Fix workflow:
#   1. Remove the line for the operation you fixed.
#   2. Re-run pytest -- the case must pass cleanly.
#   3. Commit both the fix and the trimmed baseline.
#
# Adding a new entry should be paired with a brief inline comment
# explaining the failure category (5xx via WDK 4xx surfacing, declared
# response schema drift, undeclared content-type, etc.).
#
# All current failures are 5xx server errors raised when fuzzed input
# reaches outbound integrations (WDK at toxodb.org / plasmodb.org / ...)
# or readiness checks. The handlers do not consistently translate
# upstream 4xx to a documented client-error response.

GET /health/ready
POST /api/v1/conversations
PATCH /api/v1/conversations/{strategyId}
POST /api/v1/conversations/{strategyId}/push
POST /api/v1/conversations/{conversation_id}/begin
POST /api/v1/conversations/step-counts
POST /api/v1/conversations/strategy-ast/normalize
PATCH /api/v1/conversations/{strategyId}/steps/{stepId}
POST /api/v1/conversations/open
POST /api/v1/conversations/{conversation_id}/save-substrategy
POST /api/v1/conversations/{conversation_id}/insert-saved
POST /api/v1/experiments/
POST /api/v1/experiments/batch
POST /api/v1/experiments/benchmark
POST /api/v1/experiments/seed
GET /api/v1/control-sets
GET /api/v1/control-sets/{control_set_id}
DELETE /api/v1/control-sets/{control_set_id}
POST /api/v1/veupathdb/auth/login
POST /api/v1/gene-sets/{gene_set_id}/export
POST /api/v1/gene-sets/import
POST /api/v1/eval/strategy-gene-ids
GET /api/v1/memories
GET /api/v1/memories/search
PATCH /api/v1/memories/{key}
DELETE /api/v1/memories/{key}
