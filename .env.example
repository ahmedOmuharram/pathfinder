# Repo-root environment file (optional)
#
# `docker-compose.yml` is runnable without this file, but providing it makes it
# easy to configure LLM provider credentials and local overrides.
#
# Copy to `.env` and fill in values:
#   cp .env.example .env

# API

# Required for real tool-calling chat
OPENAI_API_KEY=

# Optional: planning provider alternatives
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# 32+ chars (dev default exists, but set your own)
API_SECRET_KEY=change-me-32-chars-min-xxxx-xxxx

# Database/Redis/Qdrant overrides (compose already sets container-friendly defaults)
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/pathfinder
# QDRANT_URL=http://qdrant:6333
# QDRANT_API_KEY=

# Web

# For non-docker local dev you likely want:
# NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

